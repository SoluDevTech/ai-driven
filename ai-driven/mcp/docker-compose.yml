# docker-compose.mcp.yml
version: '3.8'

services:
  context7:
    image: mcp/context7:latest
    container_name: mcp-context7
    restart: unless-stopped
    ports:
      - "9021:8080"
    command: []